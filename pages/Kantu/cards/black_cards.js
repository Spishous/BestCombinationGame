/**
 * List of image cards, each line must be into " " and finish by ','.
 *
 * @author GIRARD Timothée
 * @version 1.0
 */

/**
 * 79 image cards.
 *
 * @type {*[]} the tab of image cards.
 */
const BLACK_CARDS_LIST = [];

for(i=1; i<=24; i++) {
	BLACK_CARDS_LIST.push("_(" + i + ").jpeg");
}

for(i=1; i<=45; i++) {
	BLACK_CARDS_LIST.push("_(" + i + ").jpg");
}

/**
 * x images cards.
 *
 * @type {*[]} the tab of official image cards.
 */
const BLACK_CARDS_LIST_KANTU = [
// TODO
];